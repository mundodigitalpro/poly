services:
  poly:
    build: .
    container_name: poly
    restart: unless-stopped
    volumes:
      - ./.env:/app/.env:ro
      - ./data:/app/data
      - ./logs:/app/logs
    # Uncomment to override entrypoint if needed for debugging
    # command: python poly_client.py --balance
