services:
  poly:
    build: .
    container_name: poly
    env_file:
      - .env
    # Restart policy can be adjusted depending on if it's a daemon or a one-off script
    # restart: unless-stopped
    volumes:
      - .:/app
    command: python poly_client.py
